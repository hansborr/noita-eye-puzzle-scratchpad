# Rust files over the 600-line default budget, pinned at their current size so
# they can only SHRINK. Lower the number as you remove lines; delete the line
# once the file drops to <= 600 (the ratchet enforces this). Every entry needs a
# "# reason" naming the role and the planned way it shrinks.
#
# A pin may be BUMPED UP only with a reason in the same commit (e.g. a feature
# grows a file before it is split). The bump shows up in `git diff` and is
# reviewed — never a silent carve-out.
#
# <path>                                   <max-lines>  # reason
src/attack/gak_attack/tests.rs             1702  # extracted GCTAK gate tests (synthetic fixtures); split by stage only if it grows
src/attack/gak_attack/eyes/heldout.rs      826   # indivisible held-out scoring core (embargoed-consensus scoring + matched-null tail + synthetic positive control share HeldOutScore/EyeMessageEvidence private internals); splits by stage only if it grows
src/attack/gak_attack/solver/mod.rs        797   # residual: cohesive indivisible GCTAK solve core (permutation-recovery + chain-link path tightly coupled); deck substrate already split into deck_fixture/deck_attack/deck_sweep siblings
src/ciphers/tests.rs                       992   # extracted candidate-cipher unit tests (exact round-trip controls)
src/attack/solve/tests.rs                  2040  # extracted solve-pipeline tests (unit + 8 include_str! corpus pipelines); split into tests_unit/tests_pipeline if it grows
